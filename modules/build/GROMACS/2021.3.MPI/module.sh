#!/bin/bash

source ../../../variables.sh

MODULE_NAME=GROMACS
VERSION="2021.3"
TAG="MPI"

SRC_DIR=gromacs-$VERSION
INSTALL_PREFIX=$MODULES_ROOT/${MODULE_NAME}/${VERSION}.${TAG}/$SYSTEM_NAME
MODULEFILE_SOURCE=../../../modulefiles/${MODULE_NAME}/${VERSION}.${TAG}
MODULEFILE_INSTALL_DEST=$MODULEFILES_PATH/$MODULE_NAME/${VERSION}.${TAG}

echo "Using INSTALL_PREFIX=$INSTALL_PREFIX"
echo "Modulefile source: $MODULEFILE_SOURCE"
echo "Modulefile destination: $MODULEFILE_INSTALL_DEST"
