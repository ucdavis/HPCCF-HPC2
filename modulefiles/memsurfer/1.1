#%Module1.0
## Memsurfer
## by PO
## 2022-25-7


set name "memsurfer"
set dir "memsurfer"
set os "ucdhpc-20.04"
set note "Memsurfer -MemSurfer is an efficient and versatile tool to compute and analyze membrane surfaces found in a wide variety of large-scale molecular simulations.  \n    https://github.com/LLNL/MemSurfer"
set version "1.1"

module-whatis "This module loads $name version $version for $os. $note"

setenv PLUMED_HOME /software/$dir/$name-$version/$os

prepend-path PATH $env(MEMSURFER_HOME)/bin
prepend-path LD_LIBRARY_PATH  $env(MEMSURFER_HOME)/lib
prepend-path CPATH $env(MEMSURFER_HOME)/include

if [module-info mode load] {
        puts stderr "Module [module-info name] loaded "
}
